import styled from "styled-components"

export const GenericBodyStyled = styled.div`
  display: flex;
  flex-direction: column;  // organiza header, main e footer verticalmente
  min-height: 100vh;       // ocupa pelo menos a altura da tela
  width: 100vw;
`
